import { LoginForm } from '@/features/auth/components/LoginForm/LoginForm';

export const metadata = {
  title: 'Sign In - NexusAI Research Assistant',
};

export default function LoginPage() {
  return <LoginForm />;
}
