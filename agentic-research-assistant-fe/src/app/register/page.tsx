import { RegisterForm } from '@/features/auth/components/RegisterForm/RegisterForm';

export const metadata = {
  title: 'Create Account - NexusAI Research Assistant',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
